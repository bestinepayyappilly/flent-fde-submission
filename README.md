# Flent FDE take-home — submission

Bestine Payyappilly · September 2026

The submission is in **[`flent-fde-submission.zip`](flent-fde-submission.zip)**, which
is password protected. The password is in my email.

Inside:

- `approach-note.pdf` — the approach note
- `execution-plan.pdf` — the execution plan
- `flent-trust-layer-bestine.zip` — the proof of work, the full repository

The proof of work needs no install. Unzip the inner archive and run:

```
cd flent-trust-layer/trust-layer
python3 trust_layer.py
```

Python 3.8+, standard library only.

---

This repository exists only to hand over the files, since the archive would not go
through email. Please don't share the password on.
